\begin{center}
\begin{tikzpicture}
    \begin{axis}[
        view={120}{40},
        xlabel={$x$},
        ylabel={$y$},
        zlabel={$z$},
        xmin=-5, xmax=5,
        ymin=-5, ymax=5,
        zmin=-5, zmax=5,
        samples=10, % Reduced samples
    ]
    
    \addplot3 [
        surf, 
        shader=interp, 
        domain=-2:2,    % Reduced u domain
        y domain=0:360, % v domain
        z buffer=sort,
        colormap/hot
    ] (
        {sinh(x) * cos(y)}, 
        {sinh(x) * sin(y)}, 
        {cosh(x)}
    );

    \addplot3 [
        surf, 
        shader=interp, 
        domain=-2:2,    % Reduced u domain
        y domain=0:360, % v domain
        z buffer=sort,
        colormap/hot
    ] (
        {sinh(x) * cos(y)}, 
        {sinh(x) * sin(y)}, 
        {-cosh(x)}
    );

    \end{axis}
\end{tikzpicture}
\end{center}
