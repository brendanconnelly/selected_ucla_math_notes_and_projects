\begin{center}
\begin{tikzpicture}
    \begin{axis}[
        view={120}{40}, % Adjust the viewing angle
        xlabel={$x$},
        ylabel={$y$},
        zlabel={$z$},
        xmin=-3, xmax=3,
        ymin=-3, ymax=3,
        zmin=-2, zmax=2,
        samples=30
    ]
    
    % Hyperboloid of one sheet
    \addplot3 [
        surf, 
        shader=interp, 
        domain=-2:2,    % u domain
        y domain=0:360, % v domain (in degrees)
        z buffer=sort,
        colormap/hot
    ] (
        {cosh(x) * cos(y)}, % x
        {cosh(x) * sin(y)}, % y
        {sinh(x)}           % z
    );

    \end{axis}
\end{tikzpicture}
\end{center}